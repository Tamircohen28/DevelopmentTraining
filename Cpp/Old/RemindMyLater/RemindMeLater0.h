#pragma once

namespace RML0 {
	// function to print reminder
	void print_reminder(const char* const p_reminder);

	// function to recieve reminder from user
	void rec_reminder(char*& p_reminder);

	// funtion to call RML level 0
	void reminder_in();
}